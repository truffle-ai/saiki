# Add your OpenAI API key here (must start with 'sk-' for OpenAI, not 'sk-proj-' which is Anthropic's format)
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Set the log level to debug to see more verbose output
# LOG_LEVEL=debug