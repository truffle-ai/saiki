import ChatApp from '../components/ChatApp';

/**
 * Renders the main page of the application by displaying the {@link ChatApp} component.
 */
export default function Home() {
  return <ChatApp />;
}
