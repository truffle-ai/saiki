/**
 * Type definitions for core configuration (CLI-agnostic)
 */

import type { AgentCard, StorageConfig } from './schemas.js';

// Re-export types from schemas for convenience
export type { AgentCard, StorageConfig };
